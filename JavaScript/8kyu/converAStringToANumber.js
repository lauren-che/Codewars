// https://www.codewars.com/kata/544675c6f971f7399a000e79/train/javascript

// We need a function that can transform a string into a number. 

// I am creating a constant variable called stringToNumber and storing a function inside.
const stringToNumber = function(str){
    return parseInt(str); // when a string is passed through variable str parseInt will return a number
}